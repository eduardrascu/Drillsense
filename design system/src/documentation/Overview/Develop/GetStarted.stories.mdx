import { Meta } from '@storybook/blocks';

import { FlexContainer, Title, Paragraph, Link } from '../../blocks/styles';

<Meta title="Overview/Develop/Get Started" />

<FlexContainer $gap={64}>
  <FlexContainer $gap={16}>
    <Title>Installation</Title>
    <Paragraph>Install the package by running:</Paragraph>

    ```bash
    # If you use npm:
    npm install @fusestudio --save

    # If you prefer Yarn, use the following command instead:
    yarn add @fusestudio
    ```

  </FlexContainer>
  <FlexContainer $gap={40}>
    <FlexContainer $gap={16}>
      <Title>Configure your project</Title>
      
        Because Storybook works separately from your app, you&apos;ll need to
        configure Storybook for your specific stack and setup. Below, explore
        guides for configuring Storybook with popular frameworks and tools. If
        you get stuck, learn how you can ask for help from Storybook community.
    </FlexContainer>
    <FlexContainer $gap={8}>
      <Link href="#">Add styling and CSS</Link>
      <Link href="#">Provide context and mocking</Link>
      <Link href="#">Load assets and resources</Link>
      <Link href="#">Storybook documentation</Link>
    </FlexContainer>
  </FlexContainer>
</FlexContainer>
